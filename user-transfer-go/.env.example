# Konfigurasi koneksi database Postgres.
# Salin file ini menjadi ".env" lalu sesuaikan nilainya.

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=user_transfer
DB_SSLMODE=disable

# Port aplikasi (opsional, default 8080 jika kosong)
APP_PORT=8080
